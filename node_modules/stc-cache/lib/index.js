'use strict';

exports.__esModule = true;

var _promise = require('babel-runtime/core-js/promise');

var _promise2 = _interopRequireDefault(_promise);

var _classCallCheck2 = require('babel-runtime/helpers/classCallCheck');

var _classCallCheck3 = _interopRequireDefault(_classCallCheck2);

var _stcHelper = require('stc-helper');

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var noop = function noop() {};

/**
 * cache
 */

var Cache = function () {
  /**
   * constructor
   */

  function Cache() {
    var options = arguments.length <= 0 || arguments[0] === undefined ? {
      path: '',
      type: 'default',
      onlyMemory: false,
      handle: null,
      logger: null
    } : arguments[0];
    (0, _classCallCheck3.default)(this, Cache);

    if (typeof options === 'string') {
      options = { type: options };
    }
    this.options = options;
    this.path = this.getStorePath();
    this.cache = {};
    this.handle = options.handle || JSON;
    this.logger = options.logger || noop;
    this.onlyMemory = options.onlyMemory || false;
  }
  /**
   * get store path
   */


  Cache.prototype.getStorePath = function getStorePath() {
    if (!this.options.path) {
      throw new Error('cache path must be set');
    }
    var p = this.options.path + '/' + this.options.type;
    (0, _stcHelper.mkdir)(p);
    return p;
  };
  /**
   * get saved file path
   */


  Cache.prototype.getSavedFilePath = function getSavedFilePath(name) {
    if (!this.path) {
      return;
    }
    var savePath = name[0] + '/' + name[1] + '/' + name + '.json';
    var p = _path2.default.join(this.path, savePath);
    (0, _stcHelper.mkdir)(_path2.default.dirname(p));
    return p;
  };
  /**
   * get cache
   */


  Cache.prototype.get = function get(name) {
    var _this = this;

    var encoding = arguments.length <= 1 || arguments[1] === undefined ? 'utf8' : arguments[1];

    if (this.cache[name] !== undefined || this.onlyMemory) {
      return _promise2.default.resolve(this.cache[name]);
    }
    var filePath = this.getSavedFilePath(name);
    if (!filePath || !(0, _stcHelper.isFile)(filePath)) {
      return _promise2.default.resolve();
    }
    var fn = (0, _stcHelper.promisify)(_fs2.default.readFile, _fs2.default);
    return fn(filePath, encoding).then(function (data) {
      if (!data) {
        return;
      }
      data = _this.handle.parse(data);
      _this.cache[name] = data;
      return data;
    }).catch(function () {
      _this.logger(new Error('get cache error ' + name));
    });
  };
  /**
   * set cache
   */


  Cache.prototype.set = function set(name, value) {
    var _this2 = this;

    this.cache[name] = value;
    if (this.onlyMemory) {
      return;
    }
    var filePath = this.getSavedFilePath(name);
    if (!filePath) {
      return;
    }
    var fn = (0, _stcHelper.promisify)(_fs2.default.writeFile, _fs2.default);
    value = this.handle.stringify(value);
    return fn(filePath, value).catch(function () {
      _this2.logger(new Error('set cache error ' + name));
    });
  };

  return Cache;
}();

exports.default = Cache;
//# sourceMappingURL=index.js.map