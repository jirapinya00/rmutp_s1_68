 DATABASE_NAME =
    DATABASE_USER = 
    DATABASE_PASSWORD = 

   
    DATABASE_PORT= 
   
    DATABASE_URL= DATABASE_URL="postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@localhost:${DATABASE_PORT}/${DATABASE_NAME}"